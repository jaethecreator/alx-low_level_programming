#include <stdio.h>

/**
 * main - entry point
 * test and fuck off
 * Return: Always 0 (success)
 */
int main(void)
{
char *s = "\"Programming is like building a multilingual puzzle";

puts(s);
return (0);
}
